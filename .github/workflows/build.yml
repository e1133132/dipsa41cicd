name :Golang web example
on:
   push:
        branches:
        -'v1.0'
        tags:
        -'latest'

jobs:
  ci:
       runs-on: ubuntu-latest
       permission: 
         contents: write
       steps:
         -name: Checkout
          uses: action/checkout@v4

